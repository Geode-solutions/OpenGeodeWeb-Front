import type { TableDefinition } from "./table_definition";

export const dataTable: TableDefinition = {
  name: "data",
  schema: "id, name, viewer_type, geode_object_type, visible, created_at, binary_light_viewable",
};
